# Protomaps

Metti qui il file vettoriale `.pmtiles` usato dalla Control Room.

Esempio locale:

```env
NEXT_PUBLIC_PROTOMAPS_PM_TILES_URL=/maps/ischia.pmtiles
```

Non committare file `.pmtiles`: possono essere grandi e sono gia esclusi da `.gitignore`.
